<p>
  <strong>Name:</strong>
  <%= @setup.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @setup.description %>
</p>

<p>
  <strong>Hero image:</strong>
  <%= image_tag(@setup.hero_image_id, width: 600) if @setup.hero_image_id.present? %>
</p>

<p>
  <strong>User Id:</strong>
  <%= @setup.user_id %>
</p>

<%= link_to 'Edit', edit_setup_path(@setup) %> |
<%= link_to 'Back', setups_path %>
